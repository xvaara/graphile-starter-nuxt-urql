{
  "client": {
    "service": {
      // can be a string pointing to a single file or an array of strings
      "localSchemaFile": "./data/schema.graphql"
    },
    "includes": [
      "**/*.{gql,graphql}"
    ]
  }
}
